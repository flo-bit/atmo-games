# @svelte-atproto/oauth
# Generate both secrets with: pnpm atproto:setup
ORIGIN=
CLIENT_ASSERTION_KEY=
COOKIE_SECRET=

# Optional: use the local Contrail API instead of https://api.atmo.games
# CONTRAIL_URL=http://127.0.0.1:8787
